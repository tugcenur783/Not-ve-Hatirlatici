<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hatirlatici">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hatirlatici">
        <activity android:name=".NotOlustur"></activity>
        <activity android:name=".Takvim1" />
        <activity android:name=".Takvim2" />
        <activity android:name=".Not_Defteri" />
        <activity android:name=".KayitOl" />
        <activity android:name=".AnaMenu" />
        <activity android:name=".LoginPanel" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>